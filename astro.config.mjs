import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // 用户主页仓库：https://berryuiki.github.io/（根路径）
  site: 'https://berryuiki.github.io',
  base: '/',
});
